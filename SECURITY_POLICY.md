# 猫猫天团 高危命令审批规程

**制定人**: 平安喵 (ping-an-miao) · 安全工程师  
**生效日期**: 2026-04-24  
**版本**: v1.0

---

## 核心原则

**高危命令必须先审后跑。** 任何 Agent 在执行下列类别的命令之前，须先向平安喵（ping-an-miao）发送审批请求，等待明确的"批准"回复后方可执行。

---

## 高危命令分类

### 🔴 A类 — 数据破坏型（严格禁止不经审批执行）

| 命令/操作 | 风险说明 |
|-----------|---------|
| `rm -rf` / `rimraf` | 递归删除，不可恢复 |
| `DROP TABLE` / `DROP DATABASE` | 数据库表/库级删除 |
| `git reset --hard` | 丢弃未提交更改 |
| `git push --force` / `git push -f` | 覆盖远端历史 |
| `git branch -D` | 强制删除分支 |
| `> file` / `truncate` | 文件覆盖清空 |
| 删除 `.env` / 密钥文件 | 凭据不可恢复 |

### 🟠 B类 — 权限/网络型（需审批）

| 命令/操作 | 风险说明 |
|-----------|---------|
| `chmod 777` / `chown` 递归 | 权限放开，攻击面扩大 |
| `curl` / `wget` 写入系统目录 | 远程代码下载 |
| `npm install --global` | 全局依赖篡改 |
| 修改 `/etc/hosts` / `/etc/passwd` 等系统文件 | 系统级配置变更 |
| 开放端口绑定 `0.0.0.0` | 暴露服务至公网 |
| 向外部服务发送真实用户数据 | 数据隐私 |

### 🟡 C类 — 不可逆部署型（需审批）

| 命令/操作 | 风险说明 |
|-----------|---------|
| 生产环境 `git push` | 影响真实用户 |
| 数据库 `migration` (ALTER TABLE 删列) | 数据结构不可逆 |
| 发布 npm/pip 包 | 影响下游依赖方 |
| 删除 CI/CD 配置 | 破坏自动化流水线 |

---

## 审批流程

```
Agent 发现需要执行高危命令
        │
        ▼
向平安喵发送审批请求（见格式）
        │
        ▼
平安喵评估 → 批准 / 拒绝 / 要求修改
        │
   批准 ┤ 拒绝
        │      └─→ Agent 放弃或采用替代方案
        ▼
Agent 执行命令，完成后回报结果
```

### 审批请求格式

```
clawteam inbox send my-maomao ping-an-miao \
  "【高危审批】命令: <具体命令> | 原因: <为什么需要执行> | 影响范围: <哪些文件/数据库/服务> | 可回滚: <是/否，如何回滚>"
```

### 平安喵响应时限

- **A 类命令**: 10 分钟内响应
- **B/C 类命令**: 30 分钟内响应
- 紧急情况可向总裁喵（zong-cai-miao）升级

---

## 豁免情形

以下情况可免申请（事后通知即可）：

- 在 `--dry-run` 模式下的预演（不实际执行）
- 个人 worktree 内的 `git reset --hard`（不影响共享分支）
- 删除明确标记为临时文件（`*.tmp`、`*.log`）的操作

---

## 违规处理

Agent 绕过审批擅自执行高危命令视为安全事故。平安喵有权：

1. 立即暂停该 Agent 的写入权限
2. 向总裁喵上报
3. 要求事故回顾（Postmortem）

---

## 联系方式

- 审批请求: `clawteam inbox send my-maomao ping-an-miao "【高危审批】..."`
- 安全事件上报: `clawteam inbox send my-maomao ping-an-miao "【安全事件】..."`
- 紧急升级: `clawteam inbox send my-maomao zong-cai-miao "【紧急安全】..."`
